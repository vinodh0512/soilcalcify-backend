-- Create the database if it does not exist
-- Change the database name if needed
CREATE DATABASE IF NOT EXISTS `u352373478_SoilCalcify`
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

-- Switch to the database (optional for CLI sessions)
USE `u352373478_SoilCalcify`;